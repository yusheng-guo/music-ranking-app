import 'package:flutter/material.dart';

class MusicRanking extends StatelessWidget {
  const MusicRanking({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      // height: 200,
      color: Colors.blue,
    );
  }
}
